{
  "name": "rosync-extension",
  "displayName": "RoSync",
  "description": "RoSync editor integration foundation for VS Code.",
  "version": "0.1.0",
  "publisher": "rosync",
  "private": true,
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:rosync.explorer",
    "onView:rosync.properties",
    "onView:rosync.status",
    "onView:rosync.plan",
    "onView:rosync.conflicts",
    "onView:rosync.git",
    "onView:rosync.agent"
  ],
  "main": "./dist/extension.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.19",
    "@types/vscode": "^1.95.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.3"
  },
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "rosync-sidebar",
          "title": "RoSync",
          "icon": "assets/rosync-icon.svg"
        }
      ]
    },
    "views": {
      "rosync-sidebar": [
        {
          "id": "rosync.explorer",
          "name": "Explorer"
        },
        {
          "id": "rosync.properties",
          "name": "Properties",
          "type": "webview"
        },
        {
          "id": "rosync.status",
          "name": "Sync Status"
        },
        {
          "id": "rosync.plan",
          "name": "Sync Plan"
        },
        {
          "id": "rosync.conflicts",
          "name": "Conflicts"
        },
        {
          "id": "rosync.git",
          "name": "Git History"
        },
        {
          "id": "rosync.agent",
          "name": "AI Agent Log"
        }
      ]
    },
    "commands": [
      {
        "command": "rosync.refreshExplorer",
        "title": "Refresh RoSync Explorer"
      },
      {
        "command": "rosync.openSource",
        "title": "Open Source"
      },
      {
        "command": "rosync.copyPath",
        "title": "Copy Roblox Path"
      },
      {
        "command": "rosync.createInstance",
        "title": "Create Instance"
      },
      {
        "command": "rosync.renameNode",
        "title": "Rename Instance"
      },
      {
        "command": "rosync.deleteNode",
        "title": "Delete Instance"
      },
      {
        "command": "rosync.refreshConflicts",
        "title": "Refresh RoSync Conflicts"
      },
      {
        "command": "rosync.clearSyncPlan",
        "title": "Clear Sync Plan"
      },
      {
        "command": "rosync.resolveConflictOurs",
        "title": "Keep Disk Version"
      },
      {
        "command": "rosync.resolveConflictTheirs",
        "title": "Keep Studio Version"
      },
      {
        "command": "rosync.dismissConflict",
        "title": "Dismiss Conflict"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "rosync.refreshExplorer",
          "when": "view == rosync.explorer",
          "group": "navigation"
        },
        {
          "command": "rosync.createInstance",
          "when": "view == rosync.explorer",
          "group": "navigation@1"
        },
        {
          "command": "rosync.refreshConflicts",
          "when": "view == rosync.conflicts",
          "group": "navigation"
        },
        {
          "command": "rosync.clearSyncPlan",
          "when": "view == rosync.plan",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "rosync.openSource",
          "when": "view == rosync.explorer",
          "group": "inline"
        },
        {
          "command": "rosync.copyPath",
          "when": "view == rosync.explorer",
          "group": "inline@1"
        },
        {
          "command": "rosync.createInstance",
          "when": "view == rosync.explorer",
          "group": "navigation"
        },
        {
          "command": "rosync.renameNode",
          "when": "view == rosync.explorer",
          "group": "navigation@1"
        },
        {
          "command": "rosync.deleteNode",
          "when": "view == rosync.explorer",
          "group": "navigation@2"
        },
        {
          "command": "rosync.resolveConflictOurs",
          "when": "view == rosync.conflicts && viewItem == rosyncConflict.item",
          "group": "inline"
        },
        {
          "command": "rosync.resolveConflictTheirs",
          "when": "view == rosync.conflicts && viewItem == rosyncConflict.item",
          "group": "inline@1"
        },
        {
          "command": "rosync.dismissConflict",
          "when": "view == rosync.conflicts && viewItem == rosyncConflict.item",
          "group": "navigation"
        }
      ]
    }
  }
}
